class Quote{
    constructor(quote, character, episode){
    this.quote = quote
    this.character = character
    this.episode = episode
    }
// instance method for rendering quote
renderQuote(){
    let quoteBox = document.getElementById("quote")
    quote.innerHTML +=
    `${this.quote}`

}

renderCharacter(){
    let charBox = document.getElementById("character")
    character.innerHTML +=
    `${this.character}`
}}

    // let episodeBox = document.getElementById("episode")
    // episode.innerHTML +=
    // `${this.episode}`